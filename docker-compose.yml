name: pa9es

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: pa9es
    container_name: pa9es
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - pa9es-data:/app/data

volumes:
  pa9es-data:
