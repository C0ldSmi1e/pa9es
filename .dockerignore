# Build artifacts and installs — recreated inside the image
node_modules
.next
tsconfig.tsbuildinfo

# Generated by scripts/copy-monaco.ts (the builder stage reruns it)
public/monaco
public/monaco-vim

# Secrets and local state — the runtime env comes from compose env_file, and
# the SQLite database lives on the data volume, never in the image
.env
.env.*
!.env.example
data

# Repo noise
.git
.gitignore
.DS_Store
docs
Dockerfile
docker-compose.yml
